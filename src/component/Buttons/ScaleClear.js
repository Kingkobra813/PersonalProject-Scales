import React, { Component } from "react"
import { Button } from "react-bootstrap"

class ScaleClear extends Component {
    constructor(props) {
        super(props)

        this.state = {

        }
    }

    render() {
        return (
            <div>
                <Button>Clear</Button>
            </div>
        )
    }
}

export default ScaleClear;